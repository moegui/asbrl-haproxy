---
- name: example
  gather_facts: true
  hosts: "localhost"
  order: sorted

  tasks:
    - name: Run ha proxy
      include_role:
        name: asbrl-haproxy
      vars:
        HA_CONFIG: /mnt/c/git/moegui/asbrl/asbrl-haproxy/test/haproxy.cfg
        HA_PORT: '8080'